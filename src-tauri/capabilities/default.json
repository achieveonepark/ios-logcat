{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the log viewer window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
